<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.EmpMapper">
    <!-- 根据id修改员工基本信息-->
    <!--set标签作用：会自动生成set关键字；会自动删除更新字段后多余的"，" -->
    <update id="updateById">
        UPDATE emp
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="job != null">
                job = #{job},
            </if>
            <if test="salary != null">
                salary = #{salary},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="entryDate != null">
                entry_date = #{entryDate},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteByIds">
<!-- 根据id批量删除员工基本信息-->
        delete from emp where id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
    </delete>

    <select id="list" resultType="com.itheima.pojo.Emp">
        select e.*,d.name deptName from emp e left join dept d on e.dept_id = d.id
        <where>
                <if test="name != null and name != ''">
                    e.name like concat('%',#{name},'%')
                </if>
                <if test="gender != null">
                    and e.gender = #{gender}
                </if>
                <if test="begin != null and end != null">
                    and e.entry_date between #{begin} and #{end}
                </if>
        </where>
        order by e.update_time desc
    </select>

    <!-- 定义resultmap -->
    <!-- 封装员工基本信息 -->
    <resultMap id="empResultMap" type="com.itheima.pojo.Emp">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/><result column="name" property="name"/>
        <result column="gender" property="gender"/><result column="phone" property="phone"/><result column="job" property="job"/>
        <result column="salary" property="salary"/><result column="image" property="image"/><result column="entry_date" property="entryDate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    <!-- 封装员工工作经历 -->
        <collection property="exprList" ofType="com.itheima.pojo.EmpExpr">
            <id column="ee_id" property="id"/>
            <result column="ee_empid" property="empId"/>
            <result column="ee_begin" property="begin"/><result column="ee_end" property="end"/>
            <result column="ee_company" property="company"/><result column="ee_job" property="job"/>
        </collection>
    </resultMap>


    <select id="getById" resultMap="empResultMap">
        <!-- 根据id查询员工信息-->
        select e.*,
               ee.id ee_id,
               ee.emp_id ee_empid,
               ee.begin ee_begin,
               ee.end ee_end,
               ee.company ee_company,
               ee.job ee_job
        from emp e left join tlias.emp_expr ee on e.id = ee.emp_id where e.id = #{id};
    </select>
    <select id="countEmpJobData" resultType="java.util.Map">
        <!-- 员工职位统计case流程控制函数-->
        select(
                  case
                      when job = 1 then '班主任'
                      when job = 2 then '讲师'
                      when job = 3 then '学工主管'
                      when job = 4 then '教研主管'
                      when job = 5 then '咨询师'
                      else '其他' end) pos,
              count(*) num from emp group by emp.job order by num ;
    </select>
    <select id="countEmpGenderData" resultType="java.util.Map">
        select
            if(gender = 1,'男性员工','女性员工') name
             ,count(*) value from emp group by gender;
    </select>
    <select id="countStudentData" resultType="java.util.Map">
        <!--  班级每个学生统计数量case流程控制函数-->
        select (
            case
                when clazz_id = 1 then 'JavaEE就业168期'
                when clazz_id = 2 then 'JavaEE就业169期'
                when clazz_id = 3 then 'JavaEE就业170期'
                when clazz_id = 4 then 'JavaEE就业166期'
                when clazz_id = 5 then 'JavaEE就业165期'
                else '其他班' end ) clz
             ,count(*) num from student s group by s.clazz_id order by num;
    </select>
    <select id="countStudentDegreeData" resultType="java.util.Map">
        <!--学员最高学历统计 -->
        select (
        case
        when s.degree = 1 then '初中'
        when s.degree = 2 then '高中'
        when s.degree = 3 then '大专'
        when s.degree = 4 then '本科'
        when s.degree = 5 then '硕士'
        when s.degree = 6 then '博士'
        else '其他' end ) deg
        ,count(*) num from student s group by degree ;
    </select>
</mapper>